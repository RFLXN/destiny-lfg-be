model User {
    id         BigInt             @id
    bungieId   BigInt?            @unique
    createdLFG LFG[]
    joinedLFG  LFGJoinedMember[]
    alteredLFG LFGAlteredMember[]

    @@index([bungieId], type: BTree)
}
